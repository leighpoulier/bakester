<div class="d-flex">
  <h1>My Bakes</h1>
  <div class="flex-grow-1"></div>
  <%= button_to "Add new Bake", :new_bake, method: :get, class: 'btn btn-secondary shadow' %>
</div>

<%= render 'list', bakes: @bakes %>

<div class='py-4'>
  <%= button_to "Add new Bake", :new_bake, method: :get, class: 'btn btn-secondary shadow' %>
</div>